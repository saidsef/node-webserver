apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/saidsef/node-webserver:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/saidsef/node-webserver:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: node-webserver
        chartPath: charts/node-webserver
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/saidsef/node-webserver"
          image.tag: "{{.DIGEST_HEX}}"
